# Web App

React Router + Vite frontend and Cloudflare Worker backend for the live streaming MVP.

Main routes:

- /camera
- /director/:eventId
- /compositor/:eventId
- /score/:eventId

Backend entry:

- workers/app.ts
