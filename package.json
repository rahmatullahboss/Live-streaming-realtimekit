{
  "name": "live-streaming-realtimekit",
  "private": true,
  "version": "0.1.0",
  "description": "Browser/mobile-first live production system with RealtimeKit, cloud compositor, and RTMP export.",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "lint": "pnpm --filter web lint"
  },
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
